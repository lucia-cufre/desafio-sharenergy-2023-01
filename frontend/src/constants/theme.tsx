import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ea9999",
      contrastText: "black",
    },

    secondary: {
      main: "#808080",
    },
  },
});

export default theme;